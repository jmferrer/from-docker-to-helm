---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tetris
  namespace: tetris
spec:
  interval: 1m
  releaseName: tetris
  chart:
    spec:
      chart: ./charts/tetris
      sourceRef:
        kind: GitRepository
        name: from-docker-to-helm
        namespace: flux-system
      interval: 1m
  values:
    replicas: 1
